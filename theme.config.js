/** @type {const} */
const themeColors = {
  primary: { light: '#22c55e', dark: '#22c55e' }, // Green accent for FitSnap
  background: { light: '#0f0f0f', dark: '#0f0f0f' }, // Near-black
  surface: { light: '#1a1a1a', dark: '#1a1a1a' }, // Dark grey
  foreground: { light: '#ffffff', dark: '#ffffff' }, // White text
  muted: { light: '#a0a0a0', dark: '#a0a0a0' }, // Light grey
  border: { light: '#333333', dark: '#333333' }, // Subtle divider
  success: { light: '#22c55e', dark: '#22c55e' }, // Green
  warning: { light: '#f97316', dark: '#f97316' }, // Orange
  error: { light: '#ef4444', dark: '#ef4444' }, // Red
};

module.exports = { themeColors };
